# Generated by the launcher --debug flag (written to the CWD on each run),
# plus any manual run captures (e.g. `... | tee launcher-test.log`).
launcher-*.log

# PyInstaller build artifacts.
build/
dist/
*.spec.bak
