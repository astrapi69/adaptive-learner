# Zufluss-Stopp fuer frontend/src/styles/global.css (#1467).
#
# Eingefrorene Zeilen-Obergrenze. global.css darf nur SCHRUMPFEN.
# Nach einer Reduktions-Tranche
# (docs/audits/global-css-analysis-2026-07-08.md) diese Zahl senken.
# Legitime Token-/Fundament-Erweiterung: mit begruendetem Kommentar
# minimal anheben (Escape-Hatch wie .filesize-baseline).
#
# Erste nicht-Kommentar-Zeile = die Baseline-Zahl.
#
# 7591 -> 7506 (EXP-044 Tranche 1, #1485): 8 tote Selektoren aus dem
# #1467-Audit geloescht (Layer 5), jeder vorab per git grep gegen
# frontend/src als ungenutzt bestaetigt.
#
# 7506 -> 7497 (#1512): body.has-bottom-nav-Reservierung entfernt mit der
# mobilen BottomTabBar.
#
# 7497 -> 7509 (EXP-044 Tranche 2a, #1485): +12 reine Wrapper-Zeilen
# (6x "@layer legacy {" + 6x "}") um die Bloecke Buttons/Landing/
# Onboarding/Assessment/Dashboard/Session - Kaskaden-Reparatur, kein
# neues CSS. Tranche 4+ loescht diese Regeln samt Wrappern wieder.
# 7509 -> 7531 (EXP-044 Tranche 2b Re-Cut, #1485): +22 reine Wrapper-
# Zeilen (11x "@layer legacy {" + 11x "}") um die vom Konflikt-Audit
# (scripts/check-legacy-wrap-conflicts.py, PR #1576) als CLEAN bestaetigten
# Bloecke Rating-Group/Storage-Toggle/Progress-Table/EXP-008-Celebration/
# EXP-008-Milestone/Curriculum/RecentSessions/MethodBadges/VisualDiff/
# AnswerAnims/PWA-Banner - Kaskaden-Reparatur, kein neues CSS. Tranche 4+
# loescht diese Regeln samt Wrappern wieder.
# 7531 -> 7557 (EXP-044 Tranche 2c, #1485): +26 reine Wrapper-Zeilen
# (13x "@layer legacy {" + 13x "}") um die vom Konflikt-Audit
# (scripts/check-legacy-wrap-conflicts.py) als CLEAN bestaetigten Bloecke
# Export/AutoLoop/MultiCycle/ModelPicker/BackupCompare/Gamification/
# Badges/Streaks/Anki/Voice/Pronunciation/NotebookLM/ApiKeyValidation -
# Kaskaden-Reparatur, kein neues CSS. Tranche 4+ loescht diese Regeln
# samt Wrappern wieder.
# NICHT gewrappt (Konflikt-Audit als Utility-Konflikt): MobilePolish-9A
# (#1583), ReadAloudTTS (#1584), ChatAnalysisLoading (#1585).
# 7557 -> 7556 (#1592): die tragende Regel
# `.app-nav .nav-group-label { display: none }` (3 Zeilen) entfernt +
# 2-Zeilen-Kommentar - die Label-Sichtbarkeit steuert jetzt die NavGroup
# `hideLabel`-Prop statt eines ungelayerten CSS-Kontext-Overrides, damit die
# Region kaskaden-schuldenfrei wird. Netto -1.
# 7556 -> 7569 (EXP-044 Tranche 2c-nav, #1597): +4 reine Wrapper-Zeilen
# (2x "@layer legacy {" + 2x "}") um Navigation + Nav-Rest, plus ein
# 9-Zeilen-Begruendungskommentar fuer die neue UNGELAYERTE Sektion
# "Lesson header / summary + content-set heading". Die 4 vom
# Konflikt-Audit als Utility-KONFLIKT gemeldeten Regeln (.content-set-meta
# h2/h4, .lesson-header-set, .lesson-summary-stats, .lesson-summary-actions)
# wurden aus der Navigation-Region herausgeloest, damit Navigation +
# Nav-Rest CLEAN wrapbar werden; sie bleiben absichtlich ungelayert
# (Reconciliation = sichtbare Aenderung, spaetere Tranche). Kein neues CSS.
# NICHT gewrappt (Utility-Konflikt, ungelayert): nur noch die neue
# Lesson-header/summary-Sektion (#1597). MobilePolish-9A (#1583),
# ChatAnalysisLoading (#1585) + ReadAloudTTS (#1584) sind jetzt gewrappt (s.u.).
# NICHT gewrappt (Legacy-vs-unlayered-Legacy-Praezedenz, #1592):
# LessonMode-LandscapeNav - jetzt NICHT mehr durch Navigation/Nav-Rest
# noch durch .nav-hamburger blockiert (alle gewrappt), sondern nur noch
# durch die ungelayerten .nav-links + .app-nav (mobile @media). Erst
# wrapbar wenn auch diese gelayert sind (naechster Schritt nach #1583).
# 7569 -> 7573 (#1540): +line-height pin on .lesson-header h1.
# 7573 -> 7575 (EXP-044 Tranche 2c, #1583): +2 netto um den Block
# MobilePolish-9A (2x Wrapper-Zeile "@layer legacy {" / "}" minus die tote
# `.nav-hamburger { margin-left: auto }`, +1 Kommentarzeile). Die
# margin-left-Regel war tot: Navigation.tsx rendert den Hamburger immer mit
# dem wichtigen `ml-0!`-Utility, das gewinnt - Loeschen raeumt den einzigen
# Wrap-Konflikt weg (Konflikt-Audit: CLEAN). Kein neues CSS.
# 7575 -> 7572 (EXP-044 Tranche 2c, #1585): -3 netto um den Block
# ChatAnalysisLoading. Die einzige Konflikt-Regel `.analysis-error-inline p
# { margin:0; color:var(--error) }` geloescht und als token-Utilities
# (`m-0 text-[var(--error)]`) an den (vorher nackten) Consumer-<p> in
# ImportDetail.tsx verschoben - gleiches Rendering; danach Block CLEAN und
# gewrappt (+2 Wrapper - 4 Regel-Zeilen - 1 Leerzeile = -3). Kein neues CSS.
# 7572 -> 7571 (EXP-044 Tranche 2c, #1584): -1 netto um den Block
# ReadAloudTTS. Die einzige Konflikt-Regel `.exercise-prompt-row > p
# { flex:1 1 auto; margin:0 }` geloescht und als Utilities (`flex-auto m-0`)
# an die 5 Prompt-<p>-Consumer verschoben (matching/picture-choice/cloze/
# cloze-multiselect + shared ExercisePromptRow; alle hatten schon `m-0`,
# `flex-auto` == `flex:1 1 auto` -> gleiches Rendering); danach Block CLEAN
# und gewrappt (+2 Wrapper - 4 Regel-Zeilen +1 Kommentarzeile = -1). Kein
# neues CSS.
# 7571 -> 7565 (EXP-044 Tranche 2c-lesson, #1632): -6 netto. Die neue
# Sektion "Lesson header / summary + content-set heading" gewrappt - der
# 9-Zeilen-Begruendungskommentar (warum ungelayert) durch eine 1-Zeilen-
# Header-Zeile ersetzt + 2 Wrapper-Zeilen (@layer legacy { / }) = -6. Die 4
# vom Konflikt-Audit gemeldeten Hits sind reconciled/allowlisted: mt-3
# (ReviewSummary.tsx:87) als wertgleiches No-op entfernt (mt-3 ==
# var(--space-3) exakt, Single-Property); text-sm (LessonHeader.tsx:142)
# BEHALTEN + allowlisted - das kompilierte Utility deklariert font-size UND
# line-height, die line-height (fix 1.25rem) war unangefochten live, Entfernen
# reflowte jede Lesson-Oberflaeche (Messung: run 29340479250); post-Wrap
# gewinnt text-sm die font-size (14px statt 14.4px bei gleicher line-height);
# .content-set-meta h4 = FoldedUserLessons.tsx:56 Tag-Heuristik-False-
# Positives (das <h4> ist ein Geschwister der <ul>, kein .content-set-meta-
# Nachkomme); die Timed-Stats m-0 (LessonSummarySections.tsx:314) ist die
# EINE gewollte Aktivierung (12px->0 nur im Timed-Modus) - alle vier in
# .legacy-wrap-accepted.json mit Begruendung. Damit sind keine ungelayerten
# Utility-Konflikt-Bloecke mehr offen; es bleibt nur die #1592 Legacy-vs-
# unlayered-Legacy-Verwobenheit (LessonMode / .nav-links / .app-nav mobile
# @media). Kein neues CSS.
# 7565 -> 7566 (Concern-Split Peel 1, #1655): +1 reine Manifest-Zeile netto
# (@import "./legacy/00-head.css"; + Leerzeile, minus eine wc-l-Zaehldifferenz
# durch die end-of-file-Newline der ausgelagerten Datei). Der erste Top-down-Peel
# verschiebt Zeilen 1-140 (Header-Kommentar + :root-Token-Bloecke) nach
# styles/legacy/00-head.css; der Ratchet zaehlt seit diesem PR die SUMME
# global.css + styles/legacy/*.css, das Verschieben selbst ist
# zahlneutral. Byte-identitaets-Beweis im PR. Kein neues CSS.
# 7566 -> 7566 (Concern-Split Peel 2, #1655): zahlneutral. Zeilen 3-345
# (Base-Resets: *, html/body/#root, img/svg/pre/code, .code-block*,
# a:not(), [hidden], :focus-visible, .sr-only/.skip-to-content, button-
# und input-Basis inkl. der zwei @layer-base-Bloecke) verbatim nach
# styles/legacy/01-base.css; in global.css ersetzt die @import-Zeile den
# Block exakt zeilenneutral (-344 +2 in global.css, +342 in der Datei).
# Byte-identitaets-Beweis im PR. Kein neues CSS.
# 7566 -> 7567 (Concern-Split Peel 3, #1655): +1 reine Manifest-Zeile.
# Der erste gewrappte @layer-legacy-Block (Buttons, Zeilen 4-53) verbatim
# nach styles/legacy/02-buttons.css (-50 +2 in global.css, +50 in der
# Datei = netto +1 fuer die @import-Zeile). Byte-identitaets-Beweis im
# PR. Kein neues CSS.
# 7567 -> 7568 (Concern-Split Peel 4, #1655): +1 reine Manifest-Zeile.
# Der Landing-page-Block (Zeilen 5-127, der zweite gewrappte
# @layer-legacy-Block) verbatim nach styles/legacy/03-landing.css
# (-123 +1 in global.css, +123 in der Datei = netto +1 fuer die
# @import-Zeile). Byte-identitaets-Beweis im PR (212319 Bytes,
# byte-identisch vor/nach). Kein neues CSS.
# 7568 -> 7569 (Concern-Split Peel 5, #1655): +1 reine Manifest-Zeile.
# Der Onboarding-page-Block (Zeilen 6-89, dritter gewrappter
# @layer-legacy-Block) verbatim nach styles/legacy/04-onboarding.css
# (-84 +1 in global.css, +84 in der Datei). Byte-identitaets-Beweis
# im PR. Kein neues CSS.
# 7569 -> 7572 (Concern-Split Batch A / Peel 6, #1655): +3 reine
# Manifest-Zeilen. Drei gewrappte Bloecke verbatim ausgelagert:
# Assessment (Z. 6-214) -> 05-assessment.css, Dashboard (inkl.
# Chart-Alternativen + Step-Eval-Insights, Z. 215-495) ->
# 06-dashboard.css, Session (Z. 496-697) -> 07-session.css.
# Byte-identitaets-Beweis im PR. Kein neues CSS.
# 7572 -> 7576 (Concern-Split Batch B / Peel 7, #1655): +4 reine
# Manifest-Zeilen. Vier Regionen verbatim: QR-Scanner-Overlay
# (ungewrappt) -> 08-qr-scanner.css, Rating-Buttons -> 09-rating.css,
# Settings-Seite + Storage-Mode-Toggle -> 10-settings.css,
# Progress-Table -> 11-progress.css. Byte-identitaets-Beweis im PR.
# Kein neues CSS.
# 2026-07-15: 7576 -> 7584 (Concern-Split Batch C / Peel 8, #1655):
# +8 reine Manifest-Zeilen (@import). Navigation -> 12-navigation.css,
# Lesson-Header -> 13-lesson-header.css, Celebration (55C) ->
# 14-celebration.css, Milestone (55D) -> 15-milestone.css,
# Feedback-Intensity (55E) -> 16-feedback-intensity.css,
# Curriculum -> 17-curriculum.css, Recent-Sessions ->
# 18-recent-sessions.css, Method-Badges -> 19-method-badges.css.
# Byte-identitaets-Beweis im PR. Kein neues CSS.
# 2026-07-15: 7584 -> 7595 (Concern-Split Batch D / Peel 9, #1655):
# +11 reine Manifest-Zeilen (@import). Diff-Highlight (52B) ->
# 20-diff-highlight.css, Answer-Animations (55B) ->
# 21-answer-animations.css, PWA-Install-Prompt (9B) ->
# 22-install-prompt.css, Mobile-Polish (9A) -> 23-mobile-polish.css,
# Lesson-Mode + Landscape-Compact-Nav (#1592-Zone, bleibt unlayered)
# -> 24-lesson-mode-nav.css, Export (16D) -> 25-export-section.css,
# Auto-Loop (17C) -> 26-auto-loop.css, Multi-Cycle-Summary (17D) ->
# 27-multi-cycle-summary.css, ModelPicker (24C) -> 28-model-picker.css,
# BackupCompare (25B) -> 29-backup-compare.css, Rich-Text-Editor
# (Phase 27 inkl. Toolbar + Code-Block) -> 30-editor.css.
# Byte-identitaets-Beweis im PR. Kein neues CSS.
7595
