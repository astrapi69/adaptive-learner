# Adaptive Learner v2.10.0

**Security release - update as soon as you can.** Until this version the
launcher published the app on EVERY network interface (IPv4 and IPv6),
in all three modes. On a machine in a shared network - an office LAN, a
hotel or conference WLAN - anyone on that network could open the app
**without any authentication**, including the AI provider keys stored in
it. Measured on a running container, not inferred. The v2.9.0 notes
named this limitation; this release removes it.

From now on the app binds to `127.0.0.1` and is reachable **only from
your own computer**. Both delivery paths are fixed, not one: the
launcher binary (engine 0.26.0) and the documented compose file for
self-hosters.

**If you deliberately reach the app from another device** - your phone
on your own WLAN, for instance - that still works, but it is now a
conscious act: set `ADAPTIVE_LEARNER_BIND_ADDRESS=0.0.0.0` in your
`.env`. Only in a network you trust; everyone in it then has the same
access you do.

**Sicherheits-Release - moeglichst bald aktualisieren.** Bis zu dieser
Version veroeffentlichte der Launcher die App auf JEDER
Netzwerkschnittstelle (IPv4 und IPv6), in allen drei Betriebsarten. In
einem geteilten Netz - Buero-LAN, Hotel- oder Konferenz-WLAN - konnte
jeder in diesem Netz die App **ohne jede Anmeldung** oeffnen, samt der
darin gespeicherten KI-Anbieterschluessel. Am laufenden Container
gemessen, nicht abgeleitet. Die v2.9.0-Notizen benannten die
Einschraenkung; dieses Release beseitigt sie.

Ab jetzt bindet die App an `127.0.0.1` und ist **nur vom eigenen
Rechner** erreichbar. Beide Auslieferungswege sind behoben, nicht einer:
das Launcher-Programm (Engine 0.26.0) und die dokumentierte
Compose-Datei fuer Selbsthoster.

**Wer die App bewusst von einem anderen Geraet erreicht** - etwa vom
Handy im eigenen WLAN -, kann das weiterhin, aber es ist jetzt eine
bewusste Entscheidung: `ADAPTIVE_LEARNER_BIND_ADDRESS=0.0.0.0` in der
`.env` setzen. Nur in einem Netz, dem du vertraust; jeder darin hat dann
denselben Zugang wie du.

## Before you install

AdaptiveLearner runs in Docker. You need Docker Desktop installed and running before starting the launcher.

- [Docker installation guide (English)](https://github.com/astrapi69/adaptive_learner/blob/main/docs/help/en/install/docker-desktop.md) - includes a "Is Docker safe to install?" section
- [Docker-Installationsanleitung (Deutsch)](https://github.com/astrapi69/adaptive_learner/blob/main/docs/help/de/install/docker-desktop.md) - mit Abschnitt "Ist Docker sicher zu installieren?"

The launcher detects Docker, downloads the ready-built AdaptiveLearner image from GitHub's registry (about 110-120 MB compressed, roughly 500 MB on disk - nothing is built on your machine), and opens the app in your browser. Later starts are fast and work offline.

## Download

| Platform | File |
|----------|------|
| Windows | `adaptive-learner-launcher.exe` |
| macOS (Apple silicon) | `adaptive-learner-launcher-macos.zip` |
| Linux | `adaptive-learner-launcher` (ELF binary) |

Each platform also ships a `*.sha256` checksum next to the binary.

## Verifying downloads

```bash
# macOS / Linux
shasum -a 256 adaptive-learner-launcher-<platform>
cat adaptive-learner-launcher-<platform>.sha256
```

```powershell
# Windows
Get-FileHash -Algorithm SHA256 .\adaptive-learner-launcher.exe
Get-Content .\adaptive-learner-launcher.exe.sha256
```

The hashes must match.

If your operating system warns about an unsigned binary, see the [AdaptiveLearner installation overview](https://github.com/astrapi69/adaptive_learner/blob/main/docs/help/en/installation.md).

## What's new

### Fixed - user-visible

- **The launcher can be closed without a system tray** (#2272, engine
  docker-app-launcher#108). The downloadable binary now ships without
  the tray declaration it could never honour, and the engine decides by
  the runtime tray fact instead of the declared intent. The artifact
  property is now stated in the installation help: no tray in the
  binary, closing always quits, the tray stays a source-install option.
- **The lesson order drives the play sequence** (#2212). Reordering
  lessons in "Manage lessons" changed the list only; opening and
  continuing a set still followed the old order.
- **Editing belongs to the lesson, not the set** (#2210). In a
  multi-lesson set, Edit opens exactly the lesson whose row you used.

### Added - infrastructure

- **A gate proves that nameable doc references exist** (#2254): make
  targets, repo paths and constants named in the docs are checked
  against the Makefile, the git index and the code. Three same-day
  precedents of documentation naming things that had been deleted.
- **The publish chain re-verifies instead of re-building** an already
  published tag (#2187), so a repeated run can no longer ship an
  artifact other than the verified one.
- **Docker images carry the app version** as tag and OCI label (#2034),
  and the compose identity is env-parameterized so a second local
  instance no longer collides with a real installation (#2122).
- **A daily rollup surfaces red scheduled runs** in one place (#2225).

### Fixed - infrastructure

- The image-size gate measures the architecture it judges (#2268) and
  fails closed by name when docker is absent (#2241); the size ceiling's
  rationale is now proven rather than assumed - the same published image
  measures 12.78 MB apart on the two docker image stores (#2134).
- The docs-hygiene gate enumerates the git index instead of the file
  system, so gitignored local artifacts can no longer turn a clean tree
  red (#2217).
- The e2e suite no longer reaches the developer's real machine: the
  config directory (#2248) and the cache directory (#2263) are pinned
  inside the throwaway data dir, with the whole resolver class pinned by
  a test.
- `release-prepare` survives its own root-vs-checkout ownership mismatch
  (#2035), and the version-display sites now ride the sync-versions
  write path (#2179) instead of drifting after every release.
- The full smoke suite is live again: all six silenced specs rewritten
  against the current UI, the silencing budget spent back to zero
  (#2177).

---

## What's new

### Security

- **The app binds to loopback by default** (#2294). Two halves, both
  needed - one alone would have left the other user group exposed:
  - the launcher engine is pinned to `>= 0.26.0`, which binds
    `127.0.0.1` and makes network exposure an explicit `bind_address`
    opt-in;
  - `docker-compose.prod.yml` publishes through
    `${ADAPTIVE_LEARNER_BIND_ADDRESS:-127.0.0.1}` instead of an
    unqualified host port.
- A test pins the CLASS, not the instance: **no** port mapping in the
  compose file may publish unqualified, so a future service cannot
  quietly reintroduce the exposure.
- Every install surface, in all eight languages, now says who can reach
  the app, why the default is loopback, and the one-variable way back.

### Fixed

- **macOS blocks the downloaded binary and the docs described a route
  that no longer exists** (#2290). Newer macOS versions offer only "Move
  to Trash" and "Done" for un-notarized programs; the right-click-to-open
  path the docs listed first is gone. All eight locales now describe the
  route that works (System Settings > Privacy & Security > Open Anyway),
  say plainly that the program is not notarized by Apple, and name the
  checksum as the check the user can still make. The `xattr` command is
  deliberately NOT the documented route - only a marked footnote for
  technical users.

---

## Release-gate record (v2.10.0)

Recorded from the runs of this session, not asserted:

- `make release-test`: **green** (backend + plugins, Vitest 771 files /
  8018 tests, dexie-smoke 77 passed, manual-plan automation 134 passed,
  docs discipline 0 FAIL).
- `npx playwright test --project=smoke`: **42 passed / 1 failed / 0
  silenced**, the silenced count matching
  `e2e/.smoke-skip-baseline.json` (budget 0). The one failure is #2288,
  unchanged from v2.9.0: a dev-server-only module-resolution defect that
  predates both releases and does not affect the built artifact - the
  same gate run walked every route against a production build.
