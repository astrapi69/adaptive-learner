{
  "name": "adaptive-learner-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:hit-test": "playwright test --config=playwright.hit-test.config.ts",
    "test:hit-test-app": "playwright test --config=playwright.hit-test-app.config.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "1.61.1",
    "fflate": "0.8.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
