{
  "name": "adaptive-learner-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:hittest": "playwright test --config=playwright.hittest.config.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "1.61.1"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
