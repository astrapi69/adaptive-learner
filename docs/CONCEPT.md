# Concept

Adaptive Learner is an adaptive learning system based on the six-method
learning model described in Asterios Raptis' Medium article series
*Von Theorie zur Praxis* (German). The system identifies the user's
preferred learning style, runs AI-supported learning sessions, and
switches between methods automatically when the data says the current
method has stopped working.

The full architecture, domain model, planned plugins, hook specs and
roadmap live in
[adaptive-learner-project-reference.md](adaptive-learner-project-reference.md).

Skeleton state (Phase 1A): the Bibliogon-inherited concept document has
been replaced by this pointer. A proper concept doc lands once the new
domain models, plugins and Settings UI are in place (Phase 4).
