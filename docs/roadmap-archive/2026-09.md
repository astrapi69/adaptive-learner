# Roadmap-Archiv 2026-09

## Archived 2026-09-09

- [x] **EXP-033 — KI-gestützte Content-Validierung** (vollständig).
  Ausgeliefert: set-weite Batch-Prüfung + Report-UI + Kosten-Bestätigung
  (AIV-01..05), Content-Hash + Signatur + "AI-Checked"-Badge
  (AIV-08..11), #674/#676/#686/#687/#690/#691; **AIV-12** (Signatur bei
  Content-Änderung invalidieren) ohne eigenen Code, direkt aus AIV-10s
  Verify-on-Read-Design (`verifySignature()` /
  `badgeStatusForCachedSet()` liefern bei Hash-Mismatch `"stale"`,
  `AiCheckedBadge` rendert das sichtbar anders als "verified"; gepinnt
  in `validation-signature.test.ts`); **AIV-06** (CI-Action
  `ai_review.py` + `ai-review.yml` im Content-Repo,
  astrapi69/adaptive-learner-content#188 gemergt 2026-08-13).
  ABSCHLUSS (2026-09-09, #3060, PR #3063): **AIV-07** übernimmt die
  feldbezogenen Vorschläge des Berichts auf eigene Sets, reviewed und
  umkehrbar. Die Risiko-Abwägung, die der Owner vor dem ersten Code
  verlangt hatte, steht in EXP-033 § 8 (acht Risiken je mit Abhilfe);
  verworfen wurden ein Ein-Klick "alles korrigieren" und ein zweiter
  KI-Aufruf, der Hinweise in Werte verwandelt. Siehe
  [EXP-033](../explorations/EXP-033-ai-content-validation.md).

- [x] **DEP-ANTHROPIC-105-01**: Upgrade the ai-anthropic plugin's
  ``anthropic`` SDK 0.55 -> 0.105 (held back in v1.41.0; out of the
  ``^0.55`` caret). A 50-version 0.x jump: the plugin's tests MOCK
  the SDK, so a green suite would NOT prove ``messages.create`` still
  works. Schedule a dedicated session that exercises a REAL API call
  (live key) before bumping the pin + lock.
  RESOLUTION (2026-09-09, #3053): already shipped past the target. #2653
  (2026-08-17, #2650) pinned `anthropic = "^0.122.0"` after a changelog
  review of the plugin's whole SDK surface; the pyproject comment carries
  the review.
  - _Archived from backlog.md / ## P3 — Lower Value or Large Effort_

- [x] **DEP-MYPY-2-01**: Upgrade mypy 1.x -> 2.0 (held back in the
  v1.41.0 dep sweep — caret ``^1.20`` caps it). Major version;
  needs a dedicated migration session (new/renamed error classes,
  stricter defaults). Not urgent. Bump the pin in
  ``backend/pyproject.toml`` + every ``plugins/*/pyproject.toml``,
  re-lock, then fix the fallout under ``poetry run mypy app/``.
  RESOLUTION (2026-09-09, #3053): already shipped. Dependabot #263 bumped
  mypy 1.20.2 -> 2.1.0 (pin `>=1.20,<3.0`); the backend runs mypy 2.3.1
  under the green PR CI; no plugin pins mypy.
  - _Archived from backlog.md / ## P3 — Lower Value or Large Effort_

- [x] **PERF-EAGER-GLOBS-01**: Remaining `eager: true`
  `import.meta.glob` sites bundle all-language data into their consuming
  chunk. `src/lib/praise/phrase-picker.ts:45` (praise, 72 KB dir, lands
  in the `celebration-bus` chunk) and `dexie-storage.ts:2304`
  (plugin-config, 28 KB dir). Convert to lazy per-key loading like the
  i18n glob fix (F-1). Low impact — praise loads during lessons,
  plugin-config is small. Opportunistic. Filed from the 2026-06-03
  performance audit (F-4).
  RESOLUTION (2026-09-09, #3053): evaluated, keep eager. Measured: praise
  catalogs 34939 B raw / 5229 B gzip (8 languages), plugin-config defaults
  386 B raw / 248 B gzip (5 files); the entry's 72 KB / 28 KB were `du`
  block sizes. Lazy praise would hand the first praise of a lesson to the
  English fallback on a cache miss (`nextPraise` is synchronous in all four
  consumers); lazy plugin-config trades 248 B for five chunks and requests.
  - _Archived from backlog.md / ## P3 — Lower Value or Large Effort_

- [x] **#508 — User profile picture** — SHIPPED on `develop` (#535) and
  released with v1.80.0. Avatar in nav + About + Dashboard (`InitialsAvatar` +
  `AvatarUpload` shared primitives), both storage modes; no backend account
  system. Closed 2026-06-15; carried in the active backlog until 2026-09-09
  because the bare `#508` id does not match the archiver's task-id pattern
  (#3049).
