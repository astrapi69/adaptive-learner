# Adaptive Learner i18n catalog (skeleton — Phase 1A).
#
# Reference language. Every other catalog mirrors this structure.
# Bibliogon's ~1500 keys per catalog are gone; new keys land
# alongside the features that need them (Phase 4 introduces
# nav / dashboard / session / assessment / settings keys).

app:
  name: Adaptive Learner
  skeleton_notice: >-
    Skeleton state. Domain models, plugins and pages land in upcoming
    phases — see docs/adaptive-learner-project-reference.md.

common:
  save: Save
  cancel: Cancel
  loading: Loading…
  error: Something went wrong.
