name: arm64 runner probe
on:
  push:
    branches: [ci/2142-ghcr-publish]
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          echo "arch: $(uname -m)"
          echo "runner: $RUNNER_OS $RUNNER_ARCH"
